import type { Metadata, Viewport } from 'next'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

export const metadata: Metadata = {
  title: 'MedTax - Gestão Fiscal para Médicos PJ',
  description: 'Simplifique sua vida fiscal. Controle impostos, notas fiscais e faturamento em uma única plataforma.',
  generator: 'MedTax',
  keywords: ['médico PJ', 'impostos médicos', 'gestão fiscal', 'notas fiscais', 'simples nacional', 'plantonista'],
}

export const viewport: Viewport = {
  themeColor: [
    { media: '(prefers-color-scheme: light)', color: '#f8fafc' },
    { media: '(prefers-color-scheme: dark)', color: '#0f172a' },
  ],
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="pt-BR" className="bg-background">
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
